@AGENTS.md

## Claude Code notes

<!-- Claude Code reads CLAUDE.md, not AGENTS.md. The import above loads AGENTS.md;
     anything below is Claude-Code-specific and is appended after it. -->

- Use plan mode for changes that touch more than two files.
- Run `npm run lint && npm run typecheck && npm run build` before saying a task is done, and show the output.
